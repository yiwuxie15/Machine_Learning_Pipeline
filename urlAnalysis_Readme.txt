urlAnalysis.py

The pipeline of the program is as follow:

read in data —— process data and extract features —— train classifiers —— test performance

The program is separated by comment into corresponding parts as shown above.
All the training and test data are stored in the program. User could change parameter to train classifiers. For pre-processing, user need to manually replace the variables with other stored variable. For the test part, it also requires manually change. 